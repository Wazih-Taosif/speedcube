<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="min-h-screen flex items-center justify-center bg-gray-100">

    {% for message in get_flashed_messages() %}
    <li>{{message}}</li>
    {% endfor %}
    <div class="w-full max-w-md bg-white p-8 rounded-xl shadow">
        <h1 class="text-3xl font-bold mb-6 text-center">Create Account</h1>

        <form method="post">
            <label class="block mb-3">
                <span class="text-gray-700">Name</span>
                <input type="text" class="mt-1 w-full p-2 border rounded-lg focus:ring focus:ring-blue-300" required
                    name="name">
            </label>

            <label class="block mb-3">
                <span class="text-gray-700">Email</span>
                <input type="email" class="mt-1 w-full p-2 border rounded-lg focus:ring focus:ring-blue-300" required
                    name="email">
            </label>

            <label class="block mb-3">
                <span class="text-gray-700">Password</span>
                <input type="password" class="mt-1 w-full p-2 border rounded-lg focus:ring focus:ring-blue-300" required
                    name="password">
            </label>

            <label class="block mb-4">
                <span class="text-gray-700">Confirm Password</span>
                <input type="password" class="mt-1 w-full p-2 border rounded-lg focus:ring focus:ring-blue-300" required
                    name="confirm_password">
            </label>

            <label class="block mb-4">
                <span class="text-gray-700">Address</span>
                <input type="text" class="mt-1 w-full p-2 border rounded-lg focus:ring focus:ring-blue-300" required
                    name="address">
            </label>

            <button class="w-full bg-blue-500 text-white py-2 rounded-lg hover:bg-blue-600 transition" type="submit">
                Register
            </button>
        </form>

        <p class="text-center text-sm text-gray-600 mt-4">
            Already have an account?
            <a href="/login" class="text-blue-500 hover:underline">Login</a>
        </p>
    </div>

</body>

</html>