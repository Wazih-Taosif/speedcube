<!doctype html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Order Confirmed</title>
</head>

<body class="bg-gray-50">

    {% include "components/navbar.html.jinja" %}

    <div class="max-w-3xl mx-auto px-6 py-20 text-center">

        <!-- Success Icon -->
        <div class="flex justify-center mb-6">
            <div class="w-20 h-20 flex items-center justify-center rounded-full bg-green-100">
                <svg class="w-10 h-10 text-green-600" fill="none" stroke="currentColor" stroke-width="2"
                    viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                </svg>
            </div>
        </div>

        <!-- Message -->
        <h1 class="text-3xl font-bold text-gray-800 mb-4">
            Thank you for your order!
        </h1>

        <p class="text-gray-600 mb-8">
            Your order has been successfully placed. We’re getting it ready for shipment 🚚
        </p>

        <!-- Order Details -->
        <div class="bg-white shadow rounded-xl p-6 text-left mb-8">
            <h2 class="text-lg font-semibold mb-4">Order Summary</h2>

            <div class="flex justify-between text-gray-700 mb-2">
                <span>Order Number</span>
                <span class="font-medium">#{{ order_id }}</span>
            </div>

            <div class="flex justify-between text-gray-700 mb-2">
                <span>Total Paid</span>
                <span class="font-medium">${{ total or "0.00" }}</span>
            </div>

            <div class="flex justify-between text-gray-700 mb-2">
                <span>Payment Method</span>
                <span class="font-medium">Card</span>
            </div>

            <div class="flex justify-between text-gray-700">
                <span>Shipping Address</span>
                <span class="font-medium text-right">
                    {{ current_user.address if current_user else "Your address" }}
                </span>
            </div>
        </div>

        <!-- Actions -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href="/browse"
                class="px-6 py-3 bg-blue-600 text-white rounded-xl text-lg font-semibold hover:bg-blue-700 transition">
                Continue Shopping
            </a>

            <a href="/"
                class="px-6 py-3 border border-gray-300 rounded-xl text-lg font-semibold text-gray-700 hover:bg-gray-100 transition">
                Go to Home
            </a>
        </div>

    </div>

</body>

</html>